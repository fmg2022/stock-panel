---
// import AccesoryRow from "./AccesoryRow.astro";
// import { accesories } from "../lib/data";
import { TableBody } from "./TableBody";
---

<div class="px-4 flex justify-center items-center">
  <table
    class="min-w-full bg-zinc-100 shadow-md rounded-md table-fixed divide-y-2"
  >
    <thead class="bg-slate-50">
      <tr class="text-gray-700">
        <th class="py-3 px-4 text-left">#</th>
        <th class="py-3 px-4 text-left">Nombre</th>
        <th class="py-3 px-4 text-left">Tipo</th>
        <th class="py-3 px-4 text-left">Categorías</th>
        <th class="py-3 px-4 text-left">Tamaño</th>
        <th class="py-3 px-4 text-left">Cantidad</th>
        <th class="py-3 px-4 text-center">Estado</th>
        <th class="py-3 px-4 text-center">Imagen</th>
        <th class="py-3 px-4 text-center">Acciones</th>
      </tr>
    </thead>
    <!-- hacer .jsx este archivo, sino no funciona correctamente -->
    <TableBody client:only="react" classNameTB="text-slate-800 divide-y" />
  </table>
</div>

<!-- <tbody class="text-slate-800 divide-y">
  {
    accesories.map((accesoryItem, index) => (
      <AccesoryRow accesory={accesoryItem} index={index + 1} />
    ))
  }
</tbody> -->
<!-- <script>
  import { currentData } from "../store/stockStore";
  import AccesoryRow from "./AccesoryRow.astro";

  const $tbody = document.getElementById("accTableBody");
  const ACCESORIES = currentData.get();

  console.log("En AccesoryTable.astro ---------", currentData);
  // AccesoryRow({ accesory: ACCESORIES[0], index: 0 }).;
</script> -->
